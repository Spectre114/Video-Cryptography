spring:
  batch:
    job:
      enabled: true
    jdbc:
      initialize-schema: embedded

video:
  input-file: "input_vid.mp4"
  output-encrypted: "encryptedVideoTest.mp4"
  decrypted-video: "decryptedVideoTest.mp4"
  ecryptedImageDir: "encrypted_frames_test/"
  decryptedFrameDir: "decrypted_frames_test/"